"use strict";
console.log("inside WelcomeBackCtrl. User was probably just redirected to welcome-back.html after logging in with Google.");
app.controller("WelcomeBackCtrl", function($scope, $window, $location) {
  
  console.log("within the function being passed through WelcomeBackCtrl");
  
  });
